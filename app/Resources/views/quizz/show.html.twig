{% extends 'base.html.twig' %}

{% block body %}
    <h1>Quizz</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quizz.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ quizz.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
        Êtes-vous sur de vouloir supprimer ce quizz ? 
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer" class="waves-effect waves-light btn">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
